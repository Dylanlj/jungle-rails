<% @order.line_items.each do |item| %>
<!-- need to fix the money format -->
 <tr>
    <td>
      <%= image_tag item.product.image.tiny %>
    </td>
    <td>
      <h4><%= item.product.name %></h4>
      <br>
      <p><%= item.product.description %></p>
    </td>
    <td>
      <%= item.total_price_cents %>
    </td>
    <td>
      quantity <%= item.quantity %>
    </td>

  </tr>
<% end %>
